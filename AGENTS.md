# NIMBY Rails MCP：工作约束与文档索引

本文件是项目主入口，只保留当前约束和操作文档索引。使用 uv 管理 Python，仓库位于 `D:\Documents\Codes\nimby-runtime-mcp`。

## 文档索引

| 文档 | 何时阅读 |
| --- | --- |
| [创建一条线路：完整流程](docs/create-line.md) | 从需求、蓝图、配线到车队、通车验证及中断恢复 |
| [OSM 数据与线路几何](docs/osm-data.md) | 获取 relation、校验站序、投影坐标、生成站台和区间 |
| [默认参数与接口支持范围](docs/defaults.md) | 选择轨道类型、层级、标签、命名、车辆及运行参数 |
| [README](README.md) | 安装、启动、代码入口及开发检查 |
| [CHANGELOG](CHANGELOG.md) | 查看项目功能和修复记录 |

## 工作约束
- 北京线路完成蓝图、曲线和原生建造检查后可自行 build，并继续通车验证；用户已授权后续线路自动建造，无需逐条确认。操作前读取现场，保留玩家手动修改。
- 自动切线计算目前仅作为候选预览，未经曲线核对不得批量应用到玩家调整过的线路；建造检查无阻塞不能替代曲率和视觉验收。
- 同一换乘站通过 Track properties 将对应轨道指定到同一 station；不要求 footprint 重叠，不为合站补建 Platform footprint extension 或站外步行链接。合站后读回各站台的 station_id，并更新检查点。
- 游戏中的经营、建造、票价、车队和命名操作使用真实 MCP 调用及游戏原生命令。Computer Use 仅用于开发接口、观测和核对。
- 使用正常资金；不得启用无限资金或写入资金余额。当前北京线路任务允许原生建造／采购形成负余额；资金不足时保留欠款，不专门等待运营回款。
- 当前北京票价采用起步25、每公里5；保留玩家票价调整，不恢复旧脚本中的每公里10。
- 暂时不新建车辆段及联络线／库线；既有设施和玩家改动先读回核实，正线建设不得顺带建成暂缓范围的蓝图。
- 常规非环线建设站后折返空间和配线，终点采用到达站台—站后折返—出发站台的进路。机场线因站后空间不足采用站前折返、终点只停一次，不能将此例外推广到常规线路。
- 密集 OSM 节点优先简化为平顺曲线，保留大致走向及必要桥隧过渡；检查反复 S 形走位和异常限速，不为贴合超近节点制造急弯。
- 正确的大弯保留；曲线修正针对不必要的左右反复 S 形扭动。半径较小本身不构成改道理由，不把整段改成端点之间的大圆滑曲线。
- 几何调整兼顾视觉效果：双线优先自然并行，避免不必要的交叉及层级切换；玩家手动整理的东直门—柳芳布局作为参考，操作前读回，不恢复旧布局。
- 默认建设规格：地铁使用 Medium speed，按实际地面／高架／地下区间还原，结合 OSM bridge、tunnel、layer 标签确定层级；城间高铁使用 High speed、高架（`depth=1`）；普速铁路使用 Medium speed、Ground（`depth=0`）。站台和道岔采用对应区段规格。用户确认的现场事实优先；清河站按 OSM 高架（`depth=1`），不要把建筑“地面层”措辞直接当作轨道 Ground。
- 修改既有轨道须经过原生 ReBP、类型／层级编辑和正常建造检查。新默认不作为自动改造玩家既有线路的指令。
- 地铁应避免导致信号阻塞的跨线同层平交；复兴门、建国门这类正线交叉采用不同地下层级。普通道岔和交叉渡线同层是正常配线，不因几何相交就改成下穿，不批量移到 `-3`。
- 改变站台层级时同步附属建筑层级；`set_track_depth` 自动同步，历史不一致使用 `sync_platform_building_depth` 修复。轨道与棕色站台／站房建筑均须读回验证。
- 当前建轨工具固定 Medium，High speed 尚未参数化。建设高铁前补齐原生 MCP 接口并验证，不能用 Medium 代替高铁后宣称满足要求。详见默认参数文档。
- 有轨电车先完成 Medium 蓝图创建、连接与配线，再用 `set_tram_track` 转为 Tram 并重新审计；不要用整线抬高／下沉规避正常地面道路交叉。Tram 编辑不等于已支持有轨电车车型采购。
- 新建车站默认使用 `Name and pax` 标签，通过 MCP 创建站台时自动设置并读回验证。
- 线路代码包含城市前缀，例如 `bj-1`、`nyc-a`；列车显示编号使用对应线路前缀和唯一序号。不要更改游戏内部 uint64 ID。
- 操作前读取当前世界；保留玩家改动，不用历史检查点覆盖线路或重放建设。检查点仅作为查找对象的种子。
- 比较经营收益时固定车队与线路，预热后测量完整 24 个游戏小时。最终候选在相同星期、相同时刻比较。按现金净增而非票面收入评估收益。
- 游戏最高速度的已核实原生值为 10000。接近测量终点时减速；超时先查询实际状态，不盲目重放建造或购车。
- 原生命令结果必须读回验证。购车后的命名失败应报告已购车辆 ID，不得再次购车作为重试。
- 新线路使用独立规划和检查点；命令前写入待处理记录，成功后保存原生 ID。检查点、协议日志和游戏状态不一致时，先核实已执行操作，再恢复。
- `build_all_blueprints` 作用于全世界全部待建蓝图；传入的节点列表只控制结果验证范围，不控制建设范围。
- 独立补建使用 `build_selected_blueprints` 的原生选区模式，列出其他待建蓝图作为保护对象，并读回轨道、附属建筑及保护对象状态。
- 新功能、修复和文档规则变更同步写入 `CHANGELOG.md` 的 `Unreleased` 区；不能把尚未实现的接口写成已有能力。
