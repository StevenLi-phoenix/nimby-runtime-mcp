# Changelog

本项目遵循 Keep a Changelog 1.1.0 和 SemVer。

## [Unreleased]

### Added

- 18 号线龙泽西西侧采用局部地下二层避开既有轨道；修整东北旺、龙泽西及霍营东附近长短段交接切线，保留源线路大致走向。

- 12 号线高家园西侧及东坝北西侧按实时交叉审计设置局部错层，附带道岔分支同步；调整长短区间交接切线后复核原生曲线。


- 增加 3、12、18 号线独立建设、折返、审计及运营入口；后续线路审计从已完成线路检查点读取原生运营 ID，仍以实时网络核对。

- 增加 19 号线独立站台、区间、站后折返、分区审计和运营入口；施工前按实时换乘调查选择层级及 station 归属。

- 17 号线局部跨线审计覆盖望京西北侧、永安里—潘家园西及十里河北侧；局部层级调整同步道岔分支，避免站前配线跨层不一致。

- 增加 18 号线马连洼—天通苑东完整 11 站规划入口；对 OSM 中五个站房中心成员投影至经过验证的轨道链，保留源坐标和偏移，超过 30 世界米拒绝自动定位。

- 增加后续 19、3、12 号线独立 OSM 规划入口，固定当前完整站序，施工前仍须重新调查既有换乘设施。

- 增加 17 号线独立站台、区间、站后折返、分区审计及运营配置入口，换乘站使用既有 station 并按现场占用层级规划。

- 增加 17 号线未来科学城北—嘉会湖完整 20 座运营站规划入口，保留经过广渠门外的区间几何，该站暂缓停靠。

- 11 号线金安桥南侧显式简化一组 26 米同层控制点，保留靠近层级过渡的控制点，避免短段切线扭动。

- 亦庄 T1 首座桥南端通过原生 Split 补齐双向地面过渡控制点，桥端保留高架层，避免游戏中点切层导致落入水域；新增 11 号线分区跨线审计入口。

- 增加 11 号线独立站台、区间、站后折返和运营配置入口；金安桥使用独立地下层并归属既有换乘站。

- 亦庄 T1 线简化亦庄同仁附近同向密集点，并将太和桥北后桥端的地面过渡点沿出线方向延后 40 世界米，保留桥体，避免 2.5 米短段扭动。

- 增加 11 号线模式口—新首钢完整 4 座运营站规划入口，支持单一连续 OSM way；模式口运营状态以现行官方站序为准。

- 增加亦庄 T1 线独立站台、区间、站后折返及双向运营配置入口，荣昌东街地面站台直接归属既有换乘站。

- 增加亦庄 T1 线定海园—屈庄完整 14 座运营站规划入口，核对荣昌东街换乘，采用 Tram 轨道转换流程并注明替代车辆限制。

- 大兴机场线显式略去机场方向一个距相邻高架控制点仅 21 米的同层冗余点，保留源区段边界与后续隧道入口。

- 增加大兴机场线独立蓝图、区间、站后折返、跨线审计和双向运营配置入口；草桥通过站台轨道归属合站。

- 增加 `set_tram_track`：通过原生 Edit 将指定蓝图轨道转换为内置 Tram，并读回类型；既有轨道仍须先 ReBP，创建和连接接口仍固定 Medium。

- 增加大兴机场线草桥—大兴机场当前 3 座运营站规划入口，独立记录桥隧来源。

- 西郊线区间简化显式处理桥隧边界附近的冗余控制点；保留隧道出入口，两座超近短桥采用短高架连接并记录范围，避免 8—26 米短轨段反复扭曲。

- 增加西郊线独立站台、区间、站后折返、分区审计及双向运营配置入口；巴沟直接归属既有 station。

- 增加西郊线巴沟—香山完整 6 站规划入口，采用国家植物园现名，保留原始地面、桥梁、隧道分类及车辆替代说明。

- 增加燕房线独立蓝图、区间、站后折返、分区交叉审计及双向运营配置入口。

- 增加燕房线阎村东—燕山完整 9 站规划入口；阎村东核对与既有房山站台的平行高架间距后共用 station，保留玩家既有设施。

- 增加 S1 线独立站台、区间、站后折返、分区交叉审计及双向运营配置入口；金安桥和苹果园直接复用既有 station，保留高架及短隧道来源。

- 增加 S1 线石厂—苹果园完整 8 站几何规划入口，保留原始 monorail/maglev 来源与桥隧标签，并注明游戏替代轨道的接口限制。

- 增加 16 号线北安河—宛平城完整 30 站 OSM 规划、独立蓝图、深层站后折返、分区审计及运营配置入口；显式归一化木樨地名称并保留原始名称，对较远换乘站另读既有站台核实层级；木樨地—达官营略去紧邻层级过渡的同层冗余控制点。

- 增加 7 号线北京西站—环球度假区完整 30 站 OSM 规划、独立蓝图、站后折返、分区审计和运营配置入口，校验双向站序和连续轨道来源。

- 增加 14 号线张郭庄—善各庄完整 33 座运营站规划、独立蓝图、站后折返、分区审计和双向运营配置入口；依据运营方时刻表排除 OSM 提前列入的红庙运营停站。

- 增加 6 号线金安桥—潞阳完整 36 站规划及独立蓝图、折返、审计和运营配置入口，包含通运门站。

- 增加 9 号线郭公庄—国家图书馆完整 13 站规划、独立蓝图、深层站后折返、分区审计和双向运营配置入口；六里桥东和军事博物馆区间保留层级过渡，略去紧邻边界的冗余控制点。

- 增加亦庄线宋家庄—亦庄火车站完整 14 站 OSM 规划、独立蓝图、分区审计和运营配置入口；宋家庄复用 station，地下深层折返创建后同步校验层级。

- 增加房山线 16 站蓝图、区间、站后折返、分区审计和双向运营配置入口，首经贸共用既有 station。

- 增加房山线阎村东—东管头南完整 16 站 OSM 规划入口及桥隧来源记录。

- 增加昌平线完整 20 站规划、独立蓝图和分区审计入口；补充 route 关系漏列的朱房北车站，并保留来源坐标与轨道投影偏移。

- 增加 15 号线站后折返、分区曲线审计及双向运营配置入口；分开读取既有线路，避免世界增长后触及单次 5,000 节点上限。

- 增加 15 号线独立站台及区间蓝图构建入口，按完整 20 站规划记录原生 ID、换乘归属与桥隧层级。
- 15号线清华东路西口—俸伯20站独立OSM规划与实时换乘调查，保留孙河至后沙峪高架和桥隧边界。
- 大兴线11站及站间双线蓝图入口，使用独立检查点，复核桥隧层级并保留公益西桥既有设施。
- 大兴线公益西桥实时端点接续及天宫院站后折返蓝图入口；创建后单独检查原生曲线及接续导致的道岔附着点重算，不自动建造。
- 大兴线独立规划入口，从贯通运营OSM关系中提取11座新站及公益西桥接续点，保留西红门高架和桥隧边界，不重画既有4号线。
- 4号线运营及站后折返入口：按实时端点和玩家层级补足两端折返空间，检查后选区建造；24站配置48个双向停靠项并先采购一列试运行车，逐命令记录恢复状态。
- 4号线站台及区间蓝图入口，按现场换乘站归属与桥隧类别创建24站，保留逐命令恢复记录和独立曲线审计。
- 4号线公益西桥—安河桥北24站独立OSM规划入口，保留桥隧来源，并与后续大兴线建设阶段区分。
- 8号线运营配置入口：35站、70个双向停靠项，终点经站后折返换到出发站台，逐命令保存原生ID并先配置一列试运行车。
- 明确曲线审核范围：保留正确大弯，只处理不必要的左右反复S形扭动，不依据小半径单独重画区间。
- 8号线区间蓝图连接入口，读取实时端点、保留桥隧类别和逐命令检查点，连接后复核层级；曲线和交叉审核独立于建造。
- 8号线站台蓝图入口，逐阶段保存原生ID、校验站台和建筑层级，并复用既有换乘车站、清理新产生的空站；不执行建造或购车。
- 8号线瀛海—朱辛庄35站独立OSM规划入口，校验连续站序并保留桥隧来源；建设前须完成实时换乘和曲线审计。
- 增加按稳定 stop ID 删除运营停站的原生 MCP 接口，读回核对剩余站序、停站 ID 和经营设置，用于终点只停一次的站内折返。
- 增加节点切线自动计算预览及原生设置工具：长短边比例达到4:1时优先保持长直段，否则使用单位向量角平分线；显式配对节点保持共同切线轴；拒绝站台、道岔、未接完端点和异常大转角，不自动移动或建造轨道。
- 首都机场线机场段主线及两条单线支路蓝图入口，基于原生曲线采样定位分岔，并保留T3—T2与T2—市区的不同走向。
- 首都机场线站台及市区双线蓝图建设入口，复用既有换乘 station，逐命令保存原生对象和待处理记录。
- 首都机场线完整双向 OSM 规划，保留北新桥延伸、T3及T2折返走向和逐段桥隧来源，校验5站站序及轨道连续性。
- 增加原生既有停站站台修改接口，限定同一车站，保留停站 ID、站序和线路经营设置并读回核验。
- 10号线站台蓝图建设入口，逐命令保存检查点，并按实时既有换乘设置分层和 station 归属。
- 10号线独立完整环线规划入口，校验45站有序停站、轨道闭环及全线地下来源标签。
- 增加全世界车站及轨道引用数查询、原生空车站删除接口；删除前拒绝任何仍被轨道引用的车站，删除后核对保留车站的引用数。
- 增加 `assign_platform_station`：通过原生 Track Edit 修改站台轨道归属，读回 station ID 并验证轨道位置、连接、层级及建成状态保持不变；已在崇文门、雍和宫、立水桥完成真实 MCP 合站验证。
- 站台范围扩展支持原生相对几何：轨道比例区间及横向偏移范围，通过Edit更新附属建筑。
- 通过原生Edit将未归属的站台范围扩展建筑绑定到站台，读取双向归属；实测绑定会按站台重新对齐建筑，保留跨站范围的相对几何参数尚待适配。
- 选区建造支持显式独立建筑ID，并读回建筑建成状态，适用于站台范围扩展建筑。
- 原生 Platform footprint extension 蓝图创建适配，校验模板、暂停状态并读回建筑和站台；合站效果仍须实际验证。
- 建筑模板目录只读MCP查询，读取运行时模板编号、名称和默认尺寸，为站台范围扩展适配提供依据。
- 5号线独立规划入口与OSM来源快照校验，验证宋家庄—天通苑北23站连续几何及桥隧标签，排除车辆段、联络线和库线。
- 13号线按玩家调整后的436节点选区完成建造，配置17站、34个双向停站和12列六节列车；运行观察及用户手动验证通过。
- 原生建造阻塞缓存只读查询，可定位报错轨道及交叉坐标；已与界面8个 water or road hits 对照。
- 13号线五道口—上地、北苑—望京西补齐局部跨路／跨水桥及过渡，原生阻塞缓存和全局蓝图界面均验证冲突清零。
- 几何规划补充视觉约束：优先自然并行、减少不必要交叉及层级切换，保留玩家整理的东直门—柳芳布局。
- 非站台蓝图控制点位置及切线方向的原生 Edit 接口，保留完整网络拓扑、轨道规格和站台位置；已在柳芳—东直门双线验证曲线平顺调整。
- 选区蓝图建造适配：原生 selection-only 标志、附属建筑选择及其他蓝图保护读回；已用53号站和苹果园方向连接线验证，保留13号线437个待建节点。
- 53号站独立 OSM 规划及非客运补建，保持既有线路停站和运营设置。
- 建设约束补充车辆段接入但暂不使用、联络线／库线建设，以及密集 OSM 节点的曲线平顺优先原则。
- 蓝图区间原生 Split 分段适配和完整网络连接验证；已验证光熙门—柳芳桥梁双线补点。站前引线仅允许在非站台一侧分段，并对原生重算造成的分支点微移采用单独容差；目标控制点位置须另行读回核对。
- 13号线独立 OSM 规划入口，验证完整17站站序和连续轨道，记录首开区段差异、来源摘要及施工前待核查项。
- 轨道层级编辑扩展至整数 -3..3；附近轨道及附属建筑查询、原生曲线交叉候选审计。
- 单线端点／分支连接及限定完整分支对的原生移除接口，用于局部配线修复。
- 以 AGENTS.md 为主索引的建线全流程、OSM 数据处理和默认参数文档，涵盖检查点恢复、配线、换乘与通车验收。
- 车站 Name and pax 标签 MCP 设置及读回验证，按复兴门手动设置确认原生枚举，保留站名、换乘及其他车站参数。
- 二号线通车核查：完整双环、18站、12列车、两处双向换乘及既有一号线保留检查；已核实内外环载客运行。
- 原生双向步行换乘接口及车站换乘关系读回；重复连接保持幂等，并遵循游戏距离限制。
- 二号线双线环线建设及内外环运营配置脚本；逐命令记录检查点，超时保留待核实标记，避免重复建设或购车。
- 二号线完整 18 站闭环 OSM 几何准备工具，检查轨道连续性、闭合及站序。
- 线路元数据支持可选 #RRGGBB 颜色，未指定时保留现有颜色，供二号线内外环分别配置。
- 二号线建设资金储备估算和完整 24 小时票价试验有效性标记；高倍速下自适应减速读回精确测量窗口。
- 票价比较按完整会计日测量，固定车队并在新票价下预热后开始；短时结果单独保留，不参与日收益排名。
- 运行时余额、游戏时间和全车队查询；以正常现金差额记录票价试验。
- 保留玩家线路调整的实时轨道盘点脚本，688 个当前节点已转换为 Medium，新增 8 列车。
- 线路城市前缀与列车编号编辑，购车按线路代码生成编号；已有列车可通过原生 Edit 改名。
- 轨道连接查询、原生 ReBP 和 Medium 蓝图类型编辑接口；新建地铁轨道及站台默认 Medium。
- 独立的运行时连接管理、版本配置和跨进程单连接锁；只读工具提供 MCP 注解。
- 无游戏依赖的回归测试和真实 stdio MCP 只读检查脚本 `smoke_readonly.py`。
- 全线12列六节编组投入客运；完成超过一个往返周期的载客验证，以及保存、重载后的36站、72停站、12列车读回校验。
- Codex 全局 `nimby_rails` MCP 接入配置、完整源码和通车存档交付说明。
- 原生轨道高度编辑工具及建成后可恢复的双向停站配置脚本。
- 线路元数据工具支持正常经营所需的起步票价及里程票价。
- 36 站地理线路的 MCP 批量构建脚本及蓝图预算检查阶段；整线使用正常资金模式。
- 线路创建、双向停站、线路命名和服务等级设置、六节编组购车工具。
- 持续 stdio MCP 测试会话，跨调用保留连接并逐条保存协议结果。
- 站台和延伸轨道的地下、地面及高架层级参数；实验性道岔连接工具。
- MCP 双站台创建、原生站名修改、既有端点连接、延伸和蓝图建成工具。
- 本地 stdio MCP 服务器及协议客户端，提供运行时状态、速度控制、轨道创建和节点查询。
- 核心取队列边界适配器，使用游戏原生工厂和所有权转移函数提交命令。
- 通过 MCP 创建地下双线轨道，读回四个新节点、坐标及双向连接的集成验证。
- 原生命令暂停/恢复测试，验证核心线程实际执行及读回结果。
- 当前游戏版本的命令 RTTI、虚函数表静态定位工具。
- 基于 Frida 的运行时桥接调查项目，使用 uv 管理依赖。
- 临时命令观测探针，记录真实建轨命令的线程、参数和调用栈，结束后卸载。
- 当前二进制版本限定的原生速度函数调用测试，验证修改并立即恢复。

### Changed

- 同步 OSM 几何文档的换乘规则：站台轨道归属同一 station，不要求额外步行连接或 footprint 建筑。
- 北京建线流程按用户授权，完成曲线及原生检查后自行建造并验证通车，不再逐线等待 build 确认；保留玩家实时改动。
- 北京线路票价基准改为起步25、每公里5，保留玩家在现有服务上的调整。
- 明确常规非环线使用站后折返；机场线受空间限制使用站前折返、终点单次停靠。8号线两端补建400米折返线与渡线。
- 北京换乘站仅通过轨道 station 归属合站，不再要求范围重叠或补建站台范围扩展建筑。
- 北京线路建造需用户明确确认，自动续跑不作为建造授权；自动切线仅作待验候选，不覆盖玩家已确认布局。
- 北京线路任务按用户新要求允许原生建造和购车保留负余额，取消为余额不足专门等待运营回款。
- 记录10号线原生选区建造允许余额变负的实测行为；预算必须独立核实，不能依赖建造命令拒绝资金不足。
- 根据玩家10号线实景调整补充几何偏好：固定站台、成对平移区间控制点、减少局部偏转和S弯，并区分折线角筛查与原生曲率验证。
- 建线流程采用统一 station 归属验证换乘，合站后扫描并删除无轨道引用的空站。
- 换乘站采用站台范围重叠合站；范围未重叠时使用 Platform footprint extension，保留既有站台位置，并要求读回合站后的站点ID。原生扩展建筑接口尚待实现。
- 按当前建设范围暂缓车辆段、联络线和库线，正线选区建造须排除暂缓蓝图并保留玩家改动。
- 地铁按实际地面／高架／地下区间还原，保留 OSM 桥隧分界；清河站按用户确认的 OSM 高架处理，轨道及附属建筑同步读回验证。
- 澄清地铁规则：处理导致信号阻塞的跨线平交，普通同层道岔与交叉渡线保留，不批量下穿。
- 明确地铁 Medium／地下且禁止平交道岔、城间高铁 High speed／高架、普速 Medium／Ground；注明未实现的类型及立交接口能力，不自动改造既有线路。
- README 改为启动和代码导航入口，旧线路脚本标注固定参数及适用限制，历史同层渡线不作为新地铁模板。
- MCP 新建车站默认采用 Name and pax 标签；标签设置失败时报告已创建对象 ID，避免重复建站。

### Fixed

- 16 号线区间检查点原子替换遇 Windows 短暂文件占用时有限重试；只重试文件提交，不重放 MCP 建造命令。
- 通过真实 MCP 修正北京5号线46个、13号线34个及10号线90个运营停站的有向站台选择，保留停站 ID、站序、车队和玩家轨道布局；2号线配置保持不变。
- 10号线区间按环线里程取模提取控制点，正确处理OSM几何起点位于丰台站—首经贸区间的情况；换乘附近保留地下分层过渡。
- 修正建筑尺寸及朝向读回字段：尺寸使用原生width/height，方向按两个float分量解码，避免错误显示为空或把方向误读为double角度。
- 持久 MCP 客户端读取结果文件遇到 Windows 临时占用或尚未写完的 JSON 时，等待同一结果文件，不重新提交建造命令。
- 站台轨道变层时同步附属建筑，修复棕色站台／站房仍停留旧层级的问题；支持单独同步历史不一致并读回验证。
- 依据游戏最高档读回值将速度上限修正为 10000；整日测试按剩余时间自适应减速，避免高速轮询跨过终点。
- 移除 MCP 原有的 100 速度限制，接口允许设置到 1000；整日测量可使用更高速度。
- 会话结果以临时文件原子发布，修复高频轮询偶发读到半写入 JSON 的问题。
- 购车先使用临时唯一编号，再从城市线路现有序号继续命名；拒绝重复列车编号，命名失败不重复购车。
- 里程票价上限按游戏界面行为限制为 10；连接测试隔离真实运行会话的锁。
- 客户端正确处理不存在对象的空 MCP 内容；只读检查不再强制旧版 72 停站数量。
- 票价参数允许高于 100 的经营试验；仍拒绝负数、非有限值和超过 10000 的输入。
- 游戏退出后的连接残留、附加失败的资源清理、关闭失败时错误卸载运行中回调的问题；失败操作不自动重放。
- 原生操作 `verified=false` 现在返回 MCP 错误，线路设置同时核验代码、票价和参考列车。
- 在执行边界重新校验轨道端点、延伸距离、参考列车和待建节点，检测数据库或模拟对象变化。
- 建造、购车和线路创建的回调只收集本次命令结果，避免混入其他命令。
- 客户端过期请求不再派发；派发前保存 processing 记录，进程中断后不自动重放。
- ID 限制为标准 ASCII uint64 字符串，避免 Unicode 数字及前导零造成读回比较错误。
- 配置脚本导入时不再读取检查点或执行游戏操作；服务器不再依赖探针测试模块。
- 调整四惠东东侧高架过渡，消除两处道路冲突；36 站全线已通过原生建造检查。
- 服务等级校正为 0 关闭、1 调车、2 客运，默认值为 2。
- 异常任务立即返回错误，关闭桥接前排空任务、拆除挂钩并等待回调退出。
- 通过反汇编和建成命令读回校正节点蓝图状态偏移，避免把站台选项当作建成状态。
- MCP 客户端输出采用 UTF-8，正确显示中文站名。
