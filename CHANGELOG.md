# Changelog

本项目遵循 Keep a Changelog 1.1.0 和 SemVer。

## [Unreleased]

### Added
- 轨道层级编辑扩展至整数 -3..3；附近轨道及附属建筑查询、原生曲线交叉候选审计。
- 单线端点／分支连接及限定完整分支对的原生移除接口，用于局部配线修复。
- 以 AGENTS.md 为主索引的建线全流程、OSM 数据处理和默认参数文档，涵盖检查点恢复、配线、换乘与通车验收。
- 车站 Name and pax 标签 MCP 设置及读回验证，按复兴门手动设置确认原生枚举，保留站名、换乘及其他车站参数。
- 二号线通车核查：完整双环、18站、12列车、两处双向换乘及既有一号线保留检查；已核实内外环载客运行。
- 原生双向步行换乘接口及车站换乘关系读回；重复连接保持幂等，并遵循游戏距离限制。
- 二号线双线环线建设及内外环运营配置脚本；逐命令记录检查点，超时保留待核实标记，避免重复建设或购车。
- 二号线完整 18 站闭环 OSM 几何准备工具，检查轨道连续性、闭合及站序。
- 线路元数据支持可选 #RRGGBB 颜色，未指定时保留现有颜色，供二号线内外环分别配置。
- 二号线建设资金储备估算和完整 24 小时票价试验有效性标记；高倍速下自适应减速读回精确测量窗口。
- 票价比较按完整会计日测量，固定车队并在新票价下预热后开始；短时结果单独保留，不参与日收益排名。
- 运行时余额、游戏时间和全车队查询；以正常现金差额记录票价试验。
- 保留玩家线路调整的实时轨道盘点脚本，688 个当前节点已转换为 Medium，新增 8 列车。
- 线路城市前缀与列车编号编辑，购车按线路代码生成编号；已有列车可通过原生 Edit 改名。
- 轨道连接查询、原生 ReBP 和 Medium 蓝图类型编辑接口；新建地铁轨道及站台默认 Medium。
- 独立的运行时连接管理、版本配置和跨进程单连接锁；只读工具提供 MCP 注解。
- 无游戏依赖的回归测试和真实 stdio MCP 只读检查脚本 `smoke_readonly.py`。
- 全线12列六节编组投入客运；完成超过一个往返周期的载客验证，以及保存、重载后的36站、72停站、12列车读回校验。
- Codex 全局 `nimby_rails` MCP 接入配置、完整源码和通车存档交付说明。
- 原生轨道高度编辑工具及建成后可恢复的双向停站配置脚本。
- 线路元数据工具支持正常经营所需的起步票价及里程票价。
- 36 站地理线路的 MCP 批量构建脚本及蓝图预算检查阶段；整线使用正常资金模式。
- 线路创建、双向停站、线路命名和服务等级设置、六节编组购车工具。
- 持续 stdio MCP 测试会话，跨调用保留连接并逐条保存协议结果。
- 站台和延伸轨道的地下、地面及高架层级参数；实验性道岔连接工具。
- MCP 双站台创建、原生站名修改、既有端点连接、延伸和蓝图建成工具。
- 本地 stdio MCP 服务器及协议客户端，提供运行时状态、速度控制、轨道创建和节点查询。
- 核心取队列边界适配器，使用游戏原生工厂和所有权转移函数提交命令。
- 通过 MCP 创建地下双线轨道，读回四个新节点、坐标及双向连接的集成验证。
- 原生命令暂停/恢复测试，验证核心线程实际执行及读回结果。
- 当前游戏版本的命令 RTTI、虚函数表静态定位工具。
- 基于 Frida 的运行时桥接调查项目，使用 uv 管理依赖。
- 临时命令观测探针，记录真实建轨命令的线程、参数和调用栈，结束后卸载。
- 当前二进制版本限定的原生速度函数调用测试，验证修改并立即恢复。

### Changed
- 澄清地铁规则：处理导致信号阻塞的跨线平交，普通同层道岔与交叉渡线保留，不批量下穿。
- 明确地铁 Medium／地下且禁止平交道岔、城间高铁 High speed／高架、普速 Medium／Ground；注明未实现的类型及立交接口能力，不自动改造既有线路。
- README 改为启动和代码导航入口，旧线路脚本标注固定参数及适用限制，历史同层渡线不作为新地铁模板。
- MCP 新建车站默认采用 Name and pax 标签；标签设置失败时报告已创建对象 ID，避免重复建站。

### Fixed
- 站台轨道变层时同步附属建筑，修复棕色站台／站房仍停留旧层级的问题；支持单独同步历史不一致并读回验证。
- 依据游戏最高档读回值将速度上限修正为 10000；整日测试按剩余时间自适应减速，避免高速轮询跨过终点。
- 移除 MCP 原有的 100 速度限制，接口允许设置到 1000；整日测量可使用更高速度。
- 会话结果以临时文件原子发布，修复高频轮询偶发读到半写入 JSON 的问题。
- 购车先使用临时唯一编号，再从城市线路现有序号继续命名；拒绝重复列车编号，命名失败不重复购车。
- 里程票价上限按游戏界面行为限制为 10；连接测试隔离真实运行会话的锁。
- 客户端正确处理不存在对象的空 MCP 内容；只读检查不再强制旧版 72 停站数量。
- 票价参数允许高于 100 的经营试验；仍拒绝负数、非有限值和超过 10000 的输入。
- 游戏退出后的连接残留、附加失败的资源清理、关闭失败时错误卸载运行中回调的问题；失败操作不自动重放。
- 原生操作 `verified=false` 现在返回 MCP 错误，线路设置同时核验代码、票价和参考列车。
- 在执行边界重新校验轨道端点、延伸距离、参考列车和待建节点，检测数据库或模拟对象变化。
- 建造、购车和线路创建的回调只收集本次命令结果，避免混入其他命令。
- 客户端过期请求不再派发；派发前保存 processing 记录，进程中断后不自动重放。
- ID 限制为标准 ASCII uint64 字符串，避免 Unicode 数字及前导零造成读回比较错误。
- 配置脚本导入时不再读取检查点或执行游戏操作；服务器不再依赖探针测试模块。
- 调整四惠东东侧高架过渡，消除两处道路冲突；36 站全线已通过原生建造检查。
- 服务等级校正为 0 关闭、1 调车、2 客运，默认值为 2。
- 异常任务立即返回错误，关闭桥接前排空任务、拆除挂钩并等待回调退出。
- 通过反汇编和建成命令读回校正节点蓝图状态偏移，避免把站台选项当作建成状态。
- MCP 客户端输出采用 UTF-8，正确显示中文站名。
